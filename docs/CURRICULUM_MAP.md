# Curriculum map

Status: canonical. This file maps calendar weeks to sprints, taught topics, capstone deliverables and acceptance criteria for both cohort branches.

## Why two branches

The same platform is built twice, on two branches derived from one reference implementation. `us-ireland` runs twelve calendar weeks with the capstone in Sprints 3 to 11. `india` runs nine, with the capstone in Sprints 3 to 10 and a separate deployment week supported by Fidelity alumni. The India modules start at applied depth and have fewer taught days per sprint, but the components follow the same order.

Folder names encode the sprint and the component, not the calendar week. There is one folder name per sprint and it is identical on every branch, so `sprint-03-trade-database` holds the Sprint 3 deliverable on `us-ireland` and on `india` alike. Nothing then has to translate a folder name: material written for one cohort cites the same path in the other, an instructor moving between rooms opens the same folder, and grading tooling matches one set of names across every branch. The calendar week a folder is taught in differs by cohort, and that mapping lives in the week table in each branch's README and in the tables below.

Folder names, fixed:

| Sprint | Folder |
|---|---|
| 3 | `sprint-03-trade-database` |
| 4 | `sprint-04-analytics-etl` |
| 5 | `sprint-05-domain-engine` |
| 6 | `sprint-06-trade-api` |
| 7 | `sprint-07-event-backbone` |
| 8 | `sprint-08-auth-service` |
| 9 | `sprint-09-trading-ui` |
| 10 | `sprint-10-extension-service` |
| 11 | `sprint-11-cloud-deploy` |

## US and Ireland, weeks 4 to 12

Twelve calendar weeks beginning with an induction week. Sprints 1 and 2 carry no capstone deliverable: they cover environment, Git, agile practice, OWASP and Secure Code Warrior, and they are taught without Copilot. The capstone starts in week 4.

| Week | Folder | Sprint | Taught topics | Capstone deliverable | Acceptance criteria |
|---|---|---|---|---|---|
| 4 | `sprint-03-trade-database` | 3, Data systems and data modelling | OLTP against OLAP; relational, document and columnar stores; advanced SQL with joins, aggregates, window functions, CTEs and subqueries; normalisation to third normal form; ER modelling; keys, constraints and indexes; Postgres and psql; Snowflake as a warehouse platform | A normalised Postgres schema for accounts, instruments, orders and positions, with constraints, indexes, structures for historical trade data, an ER diagram and representative seed data | Schema is in third normal form and version controlled as migration files. Foreign keys and check constraints present, including a unique constraint on the order idempotency key. At least three indexes justified by a named query. Seed data loads with one command and covers active, suspended and closed accounts. Every team member can walk the ER diagram unaided. Converges on `contracts/database-schema.sql` without having been given it first. |
| 5 | `sprint-04-analytics-etl` | 4, Financial services and data analytics | Investment lifecycle, risk and regulation; exploratory data analysis in pandas; cleaning, types, outliers; predictive modelling at introductory level; visualisation choice for an audience; the operational against analytical estate; accessing data through REST APIs with auth, pagination and rate limits; pytest for data work | A Python dashboard surfacing at least three business insights over a realistic trading dataset, plus ingestion structured as an extract, transform, load pipeline with tests | Three or more insights, each stated as a business claim and supported by a chart a non-technical reader can read unaided. Fauxnance candles pulled through `GET /candles/{symbol}` with the key read from the environment. Pipeline separated into extract, transform and load functions. pytest covers at least the transform step, including one malformed-input case. Rate-limit and error handling present, not a bare `try` block. |
| 6 | `sprint-05-domain-engine` | 5, Software engineering essentials, Java and OOAD | Core Java; OO principles; OOAD from requirements; UML class and sequence diagrams; SOLID with worked violations and fixes; clean code; test-driven development with red, green, refactor; JUnit 5 | The trading domain engine in Java 21: entities, enumerations, DTOs with validation, an exception hierarchy, and buy and sell rules, all unit tested | `Account`, `Instrument`, `Order` and `Position` match the domain model. `AccountStatus`, `OrderSide` and `OrderStatus` match exactly. Exception hierarchy covers all six specified cases. Business rules 1 to 8 implemented in the domain, not in a controller. No database, HTTP or Spring dependency in the domain module. Tests written before implementation, evidenced in commit history. `AccountTest`, `OrderLogicTest` and `PlaceOrderRequestValidationTest` all green. UML class and sequence diagrams committed and matching the code. |
| 7 | `sprint-06-trade-api` | 6, Software architecture and enterprise Java | Microservice decomposition and trade-offs; the role of a centralised auth service; REST design with resources, verbs, status codes, idempotency and versioning; OpenAPI-first; Spring Boot and dependency injection; layered controller, service and repository design; DTOs and Bean Validation; MyBatis mappers and parameterised queries; exception handling and structured logging; Dockerising a service; JWT validation in Spring Boot | The Trade REST API: a layered, Dockerised Spring Boot service persisting through MyBatis, implementing `contracts/trade-api.yaml`, authenticated against the provided Node auth stub | All six endpoints implemented and matching the contract, including the error envelope and every code in the catalogue. Layering enforced: no SQL in a controller, no HTTP type in the domain. MyBatis mappers use parameterised statements throughout. `@ControllerAdvice` maps every domain exception to its documented code and status. Order placement is `@Transactional`. Optimistic locking on the account version column. A protected route rejects a missing or invalid token with `AUTH-401`. Service builds and runs from a multi-stage Dockerfile. |
| 8 | `sprint-07-event-backbone` | 7, Enterprise data and engineering excellence | Batch against real-time at enterprise scale; event-driven against request-driven trade-offs; Kafka topics, producers, consumers, partitions and offsets; ETL patterns, incremental against full load, retries and idempotency; data quality, schema checks and dead-letter queues; refactoring techniques; characterisation tests; SonarQube and quality gates; DevSecOps in CI with SAST, dependency and secret scanning | Kafka topics with producers and consumers per `contracts/kafka-topics.md`; the Trade Executor filling orders against Fauxnance quotes; the market-data poller; a batch ETL pipeline loading the analytical store; the dashboard reading both sources | Three topics created with documented keys and partition counts. The Trade REST API publishes to `orders` and returns `NEW`. The Trade Executor consumes, prices against a live Fauxnance quote, applies fill or reject rules, updates order, cash and position in one transaction, and publishes to `trade-events`. Replaying a duplicate message does not double-debit an account, and the team can demonstrate that. The poller batches up to 25 symbols per call and stays inside the daily quota. Batch ETL loads `FACT_TRADES` with dead-letter handling for bad rows. SonarQube gate passing on the pipeline and the Java services. Characterisation tests written before any refactoring of the planted starter code. |
| 9 | `sprint-08-auth-service` | 8, Node.js, NestJS and authentication | Node fundamentals and the event loop; TypeScript for Node developers; NestJS modules, controllers, providers and dependency injection; layered design; DTOs with class-validator; OpenAPI-first contract design; password hashing with bcrypt or argon2; JWT structure, signing, issuance, validation and lifecycle; guards, decorators and interceptors; structured logging; auth-focused OWASP items; Jest | The NestJS auth service implementing `contracts/auth-api.yaml`, replacing the stub, protecting at least one Trade REST API route end to end | All four endpoints implemented. Claims exactly match the contract: `sub`, `accountId`, `roles`, `iat`, `exp`. Swapping the stub for the real service requires configuration changes only, no code change in the Trade REST API. Passwords hashed with argon2 or bcrypt and never logged. Refresh tokens rotate and the old one stops working. Failed login gives the same response and timing for an unknown user as for a wrong password. Jest tests cover the guard, including the expired-token and wrong-signature paths. OpenAPI docs served live by the running service. Security review against the auth-related OWASP items documented. |
| 10 | `sprint-09-trading-ui` | 9, UI development with Angular | Angular fundamentals, standalone components and signals; services and dependency injection; `HttpClient` and observables; routing and lazy loading; reactive forms and validation; form-to-API patterns; JWT interceptors; route guards; typed clients generated from OpenAPI; unit testing components and services; Playwright end-to-end testing | The Angular application: authenticated login, dashboard, order ticket and blotter, consuming typed clients generated from the contracts, with unit and Playwright tests | Login flow works end to end against the real auth service. Interceptor attaches the bearer token to every API call and does not attach it to Fauxnance or third-party calls. Route guards block unauthenticated access and redirect. API clients generated from `contracts/`, not hand-written. Order ticket validates before submission and renders every error code from the catalogue as a readable message. Blotter shows status badges and handles an order sitting at `NEW`. Playwright covers login, place order and view history. No API key and no secret present in the built bundle. |
| 11 | `sprint-10-extension-service` | 10, Applied project week | No new technology. Team-led sprint: planning, backlog, integration, architecture decision logging, cross-team review, security review, quality pass, showcase rehearsal | One extension microservice from the catalogue of six, integrated end to end, with a decision log | Extension chosen and scope confirmed on day one. Service authenticates with the platform JWT and enforces authorisation on its own routes. Integrated with at least one of Kafka or the Trade REST API, and reachable from the Angular UI. OWASP security review completed against the new service and findings addressed. SonarQube gate passing. Architecture decision log committed, giving the reasoning for each significant choice. Team can demonstrate the feature working against live data, not fixtures. |
| 12 | `sprint-11-cloud-deploy` | 11, Cloud, deployment and final showcase | Cloud against on-premise; the AWS service landscape; IAM basics and region selection; the AWS CLI; S3 buckets, objects and static hosting; origin access control; CloudFront distributions, behaviours, cache policies and invalidation; TLS and custom domains at concept level; deployment automation; cost awareness. A Fidelity platform SME briefs the cohort on Fidelity's own AWS guardrails before hands-on work | The Angular build deployed to a private S3 bucket behind CloudFront, with an automated build, upload and invalidate cycle, then the final showcase | Application reachable over HTTPS through the CloudFront distribution. S3 bucket is private and returns access denied when addressed directly. Origin access control configured, not a public bucket policy. Deployment is a single script covering build, upload and invalidation. Authenticated flows verified against the deployed front end. IAM user or role scoped to the bucket and distribution only, with no long-lived keys in the repository. Live demonstration to the panel covering the platform, the extension, design decisions, Copilot usage and the code-quality story. |

## India, weeks 2 to 9

Nine calendar weeks beginning with an induction week. The capstone starts in week 2. Sprints 1 and 2 are not delivered: fundamentals are recapped inside Sprint 3, and Copilot is introduced in Sprint 6 rather than Sprint 3. Taught days per sprint are shorter, typically three taught days plus one embedded capstone day, so the deliverables below are scoped accordingly.

| Week | Folder | Sprint | Taught topics | Capstone deliverable | Acceptance criteria |
|---|---|---|---|---|---|
| 2 | `sprint-03-trade-database` | 3, Data systems and data modelling | Fundamentals of business software development recap; OLTP against OLAP; advanced SQL with joins, aggregates, window functions, CTEs and subqueries; normalisation and ER modelling; keys, constraints and indexes; NoSQL overview; Snowflake overview | A normalised Postgres schema for accounts, instruments, orders and positions, with constraints, indexes, an ER diagram and seed data | As `us-ireland` week 4, with historical trade structures reduced to a documented design rather than a built implementation. Third normal form, constraints and the idempotency key uniqueness are still mandatory. |
| 3 | `sprint-04-analytics-etl` | 4, Financial services and data analytics | Financial services landscape; investment lifecycle, risk and regulation; exploratory data analysis in pandas; cleaning and preparation; predictive modelling at introductory level; visualisation principles; the Fidelity data landscape; accessing data through REST APIs; pytest for data work | A Python dashboard with at least three business insights, plus ingestion as an extract, transform, load pipeline with tests | As `us-ireland` week 5. Symbols must include at least two NSE or BSE instruments so that the cohort trades a market they know. |
| 4 | `sprint-05-domain-engine` | 5, Software engineering essentials, Java and OOAD | Core Java refresher; OO principles; OOAD and UML class and sequence diagrams; SOLID with worked fixes; clean code; test-driven development; JUnit 5 | The trading domain engine in Java 21: entities, enumerations, DTOs, exception hierarchy and buy and sell rules, unit tested | As `us-ireland` week 6. |
| 5 | `sprint-06-trade-api` | 6, Software architecture and enterprise Java | Microservice architecture and trade-offs; the role of a centralised auth service; Spring Boot and dependency injection; MyBatis mappers and parameterised queries; connecting Spring Boot to the Sprint 3 schema; exception handling and logging; Dockerising services; JWT validation in Spring Boot. GitHub Copilot introduced here | The Trade REST API: a layered, Dockerised Spring Boot service on MyBatis, implementing `contracts/trade-api.yaml`, authenticated against a team-provided token issuer that conforms to `contracts/auth-api.yaml` | As `us-ireland` week 7, except that there is no provided auth stub. OpenAPI-first design is assessed against the provided contract rather than a contract the team authors from scratch. |
| 6 | `sprint-07-event-backbone` | 7, Enterprise data engineering and engineering excellence | Batch against real-time data movement; event-driven architecture concepts; Kafka topics, producers, consumers, partitions and offsets; ETL patterns and idempotency; data quality and dead-letter queues; refactoring techniques and characterisation tests; SonarQube; DevSecOps in CI. **Monday is a holiday: Ganesh Chaturthi.** Four taught days | Kafka topics with producers and consumers; the Trade Executor filling orders against Fauxnance quotes; the market-data poller; a batch load into the analytical store | As `us-ireland` week 8, with the batch ETL reduced to a single incremental load into `FACT_TRADES` and its dimensions. The duplicate-delivery demonstration remains mandatory. Characterisation tests are written around the team's own Sprint 6 code, since the `india` branch ships no starter implementation. |
| 7 | `sprint-08-auth-service` | 8, Node.js, NestJS and authentication | Node fundamentals; TypeScript essentials; NestJS modules, controllers, providers and dependency injection; layered design and DTO validation; OpenAPI-first contract design; password hashing; JWT issuance and validation; guards, decorators and interceptors; structured logging; auth-focused OWASP items; Jest. Two SME cloud sessions run on Monday and Tuesday | The NestJS auth service implementing `contracts/auth-api.yaml` and protecting at least one Trade REST API route end to end | As `us-ireland` week 9, with refresh-token rotation reduced to issuing a new refresh token on each refresh; revoking the previous one is optional and should be documented if not built. |
| 8 | `sprint-09-trading-ui` | 9, UI development with Angular | Angular fundamentals, standalone components and signals; services and `HttpClient`; typed clients generated from OpenAPI; routing; reactive forms and validation; JWT interceptors and route guards; Playwright end-to-end testing. Self-directed SRE content runs on Monday. **Friday is a holiday: Gandhi Jayanti.** | The Angular application: authenticated login, dashboard, order ticket and blotter, with unit and Playwright tests | As `us-ireland` week 10, with the Playwright suite reduced to the login and place-order journeys. The no-secrets-in-the-bundle check remains mandatory. |
| 9 | `sprint-10-extension-service` | 10, Applied project | Team-led sprint: planning, backlog, integration, architecture decision logging, security review, showcase preparation | Four extensions, integrated end to end: Portfolio and P&L, Customer Preferences, Customer Notifications, and Watchlists and Price Alerts | All four built and integrated, in that dependency order. Preferences persists a default account and an alert channel per customer and applies them at next login. Notifications consumes `trade-events` and delivers through the channel Preferences holds. Watchlists monitors `market-data` and raises threshold alerts through Notifications, not to a log. Portfolio and P&L implements `contracts/portfolio-api.yaml` and prices from Fauxnance. Each service authenticates with the platform JWT. One combined OWASP security review across the four. Decision log committed. |

Holidays are marked in the table above so that week material does not plan five taught days into a four-day week. Confirm the exact calendar dates with the delivery team before publishing a dated schedule; the source deck's week commencement dates and its holiday markers do not agree.

## India deployment addendum: cloud week

India has no Sprint 11. Cloud deployment runs as a separate four and a half day week after the capstone sprint, delivered as Fidelity experiential learning and supported by Fidelity alumni rather than by Neueda instructors. Use the folder `sprint-11-cloud-deploy` for its material, in spite of there being no Sprint 11 in this cohort. The cloud week carries the same name on every branch so that material, references and grading tooling need no translation between cohorts. The README in that folder states the discrepancy in one line, so a participant who notices it reads it as a naming convention rather than an error.

| Item | Detail |
|---|---|
| Duration | Four and a half days |
| Delivered by | Fidelity, with alumni support. Neueda supplies the material and the acceptance criteria only. |
| Prerequisite | The Sprint 10 build passing its own tests, and a working local Docker Compose stack |
| Scope | Docker setup review, AWS account and IAM basics, AWS CLI, S3 static hosting, origin access control, CloudFront, deployment automation |
| Deliverable | The Angular build deployed to a private S3 bucket behind CloudFront, with an automated build, upload and invalidate cycle, and the link shared with the cohort |
| Acceptance criteria | Identical to `us-ireland` week 12, excluding the final showcase. Application reachable over HTTPS through CloudFront. Direct S3 access denied. Origin access control configured. Deployment automated in one script. Authenticated flows verified against the deployed front end. IAM scoped to the bucket and distribution, no long-lived keys committed. |
| Assessment | Capstone deployed and link shared, assessed asynchronously against the criteria above. The demonstration and assessment for the platform itself happened in week 9. |

Two constraints apply because the week is alumni-supported. Material must be self-contained, because there is no instructor to fill gaps in the room. Every step must work from a printed runbook with no live Neueda support, so write the runbook to be followed literally and test it end to end before delivery.
